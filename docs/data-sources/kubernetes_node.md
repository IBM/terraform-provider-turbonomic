---
page_title: "turbonomic_kubernetes_node Data Source - IBM Turbonomic"
subcategory: "Kubernetes"
description: |-
  Retrieves Turbonomic optimization recommendations for a Kubernetes node. Returns the pending action type — PROVISION (add capacity), SUSPEND (remove underutilized node), or RECONFIGURE (fix a NotReady node). Kubernetes nodes are modeled as VirtualMachine entities in Turbonomic; this data source filters to k8s-discovered VMs only.
---

# turbonomic_kubernetes_node (Data Source)

Retrieves Turbonomic optimization recommendations for a Kubernetes node. Returns the pending action type — PROVISION (add capacity), SUSPEND (remove underutilized node), or RECONFIGURE (fix a NotReady node). Kubernetes nodes are modeled as VirtualMachine entities in Turbonomic; this data source filters to k8s-discovered VMs only.

## Example Usage

```terraform
data "turbonomic_kubernetes_node" "example" {
  cluster = "<cluster_name>"
  name    = "<node_name>"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `cluster` (String) Name of the Kubernetes cluster as it appears in Turbonomic (e.g. `"Kubernetes-mycluster"`). Used to filter node VMs discovered by a specific cluster and exclude cloud-provider VMs with the same hostname.
- `name` (String) Name of the Kubernetes node (e.g. `"ip-10-0-1-42.ec2.internal"`).

### Read-Only

- `action_mode` (String) Mode of the pending action (e.g. `MANUAL`, `RECOMMEND`).
- `action_state` (String) State of the pending action (e.g. `READY`).
- `action_type` (String) Type of the pending action: `PROVISION`, `SUSPEND`, `RECONFIGURE`, or empty when no action exists.
- `entity_uuid` (String) Turbonomic UUID of the node (VirtualMachine) entity.
