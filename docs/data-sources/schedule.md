---
page_title: "turbonomic_schedule Data Source - IBM Turbonomic"
subcategory: "Turbonomic"
description: |-
  Returns all Turbonomic schedules, optionally filtered by display name (exact match). Schedules are referenced by UUID in turbonomic_settings_policy resources.
---

# turbonomic_schedule (Data Source)

Returns all Turbonomic schedules, optionally filtered by display name (exact match). Schedules are referenced by UUID in turbonomic_settings_policy resources.

## Example Usage

```terraform
# List all schedules
data "turbonomic_schedule" "all" {}

# Look up a specific schedule by name
data "turbonomic_schedule" "maintenance" {
  display_name = "Maintenance Window"
}

# Reference the UUID in a settings policy
resource "turbonomic_settings_policy" "with_schedule" {
  display_name  = "Scheduled VM Resize Policy"
  schedule_uuid = data.turbonomic_schedule.maintenance.schedules[0].uuid

  settings_managers = [
    {
      uuid = "resizeVmSettingsManager"
      settings = [
        {
          uuid  = "resize"
          value = "RECOMMEND"
        }
      ]
    }
  ]
}
```
<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `display_name` (String) Filter results to schedules whose display name matches this value exactly. Omit to return all schedules.

### Read-Only

- `schedules` (Attributes List) List of schedules matching the supplied filter. Empty when no schedules match. (see [below for nested schema](#nestedatt--schedules))

<a id="nestedatt--schedules"></a>
### Nested Schema for `schedules`

Read-Only:

- `display_name` (String) The human-readable name of the schedule.
- `end_date` (String) End date after which the schedule no longer recurs. Null for open-ended schedules.
- `end_time` (String) End time of the daily window in ISO8601 format.
- `next_occurrence` (String) ISO8601 local datetime of the next trigger.
- `next_occurrence_timestamp` (Number) Unix millisecond timestamp of the next trigger.
- `recurrence` (Attributes) Recurrence rule for repeating schedules. Null for one-time schedules. (see [below for nested schema](#nestedatt--schedules--recurrence))
- `remaining_time_active_ms` (Number) Milliseconds remaining in the current active window. Zero when not active.
- `start_date` (String) Start date/time of the schedule window in ISO8601 local time (YYYY-MM-DDTHH:MM).
- `start_time` (String) Start time of the daily window in ISO8601 format.
- `time_zone` (String) IANA timezone name (e.g. America/New_York).
- `uuid` (String) The UUID of the schedule.

<a id="nestedatt--schedules--recurrence"></a>
### Nested Schema for `schedules.recurrence`

Read-Only:

- `days_of_month` (List of Number) Days of the month for MONTHLY recurrence (e.g. [1, 15]).
- `days_of_week` (List of String) Days of the week for WEEKLY recurrence. Values: Mon, Tue, Wed, Thu, Fri, Sat, Sun.
- `interval` (Number) Repeat every N days/weeks/months.
- `type` (String) Recurrence type: DAILY, WEEKLY, or MONTHLY.
- `week_of_the_month` (List of Number) Which week(s) of the month for MONTHLY recurrence. -1=last, 1=first, etc.
