---
page_title: "turbonomic_user Data Source - IBM Turbonomic"
subcategory: "Turbonomic"
description: |-
  Returns Turbonomic users, optionally filtered by username, role, or login provider. Requires ADMINISTRATOR or SITE_ADMIN privileges. Sensitive fields (password, authToken) are never written to state.
---

# turbonomic_user (Data Source)

Returns Turbonomic users, optionally filtered by username, role, or login provider. Requires ADMINISTRATOR or SITE_ADMIN privileges. Sensitive fields (password, authToken) are never written to state.

## Example Usage

```terraform
# Return all administrators
data "turbonomic_user" "admins" {
  role = "ADMINISTRATOR"
}

output "admin_usernames" {
  value = [for u in data.turbonomic_user.admins.users : u.username]
}

# Lookup a single user by username
data "turbonomic_user" "jdoe" {
  username = "jdoe"
}

output "jdoe_uuid" {
  value = data.turbonomic_user.jdoe.users[0].uuid
}

# Return all LDAP users
data "turbonomic_user" "ldap_users" {
  login_provider = "LDAP"
}
```
<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `login_provider` (String) Filter results to users with this login provider: Local or LDAP.
- `role` (String) Filter results to users who have a role with this name (e.g. ADMINISTRATOR, OBSERVER).
- `username` (String) Filter results to users whose username matches this value exactly.

### Read-Only

- `users` (Attributes List) List of users matching the supplied filters. Empty when no users match. (see [below for nested schema](#nestedatt--users))

<a id="nestedatt--users"></a>
### Nested Schema for `users`

Read-Only:

- `display_name` (String) Full display name of the user.
- `login_provider` (String) Authentication provider for the user: Local or LDAP.
- `roles` (Attributes List) Roles assigned to the user. (see [below for nested schema](#nestedatt--users--roles))
- `scope_group_uuids` (List of String) UUIDs of the group scopes assigned to the user. Empty for unscoped users.
- `type` (String) User account type: DedicatedCustomer or SharedCustomer.
- `username` (String) Login username.
- `uuid` (String) The UUID of the user.

<a id="nestedatt--users--roles"></a>
### Nested Schema for `users.roles`

Read-Only:

- `description` (String) Description of the role.
- `display_name` (String) Human-readable name of the role.
- `name` (String) Role name enum value (e.g. ADMINISTRATOR, OBSERVER, AUTOMATOR).
- `uuid` (String) The UUID of the role.
