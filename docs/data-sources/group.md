---
page_title: "turbonomic_group Data Source - IBM Turbonomic"
subcategory: "Turbonomic"
description: |-
  Returns all Turbonomic groups, optionally filtered by display name (exact match) and/or group type. Omitting both filters returns every group visible to the authenticated user.
---

# turbonomic_group (Data Source)

Returns all Turbonomic groups, optionally filtered by display name (exact match) and/or group type. Omitting both filters returns every group visible to the authenticated user.

## Example Usage

### All groups

```terraform
# Look up all groups
data "turbonomic_group" "all" {}

output "group_count" {
  value = length(data.turbonomic_group.all.groups)
}

output "group_names" {
  value = [for g in data.turbonomic_group.all.groups : g.display_name]
}
```

### Filter by display name

```terraform
# Look up a specific group by name to reference its UUID in other resources
data "turbonomic_group" "prod_vms" {
  display_name = "Prod VMs"
}

output "prod_vms_uuid" {
  value = one(data.turbonomic_group.prod_vms.groups).uuid
}
```

### Filter by group type

```terraform
# Look up all VirtualMachine groups
data "turbonomic_group" "vm_groups" {
  group_type = "VirtualMachine"
}

output "vm_group_names" {
  value = [for g in data.turbonomic_group.vm_groups.groups : g.display_name]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `display_name` (String) Filter results to groups whose display name matches this value exactly. Omit to return groups of any name.
- `group_type` (String) Filter results to groups of this entity type (e.g. VirtualMachine, Storage). Omit to return groups of any type.

### Read-Only

- `groups` (Attributes List) List of groups matching the supplied filters. Empty when no groups match. (see [below for nested schema](#nestedatt--groups))

<a id="nestedatt--groups"></a>
### Nested Schema for `groups`

Read-Only:

- `active_entities_count` (Number) Number of active entities in the group.
- `class_name` (String) Internal API class name of the group object.
- `cloud_type` (String) The cloud type of the group as determined by its members (e.g. AWS, AZURE, GCP).
- `display_name` (String) The human-readable name of the group.
- `entities_count` (Number) Total number of entities in the group.
- `environment_type` (String) The environment type of the group as determined by its members. Values: CLOUD, ONPREM, HYBRID.
- `group_type` (String) The entity type that makes up the group members (e.g. VirtualMachine, Storage).
- `id` (String) The UUID of the group (same as uuid).
- `is_static` (Boolean) True if the group has a fixed member list; false if membership is defined by criteria.
- `logical_operator` (String) Logical operator applied across all criteria for dynamic groups. Values: AND, OR, XOR.
- `members_count` (Number) Number of direct members of the group.
- `severity` (String) Highest severity across member entities. Values: UNKNOWN, NORMAL, MINOR, MAJOR, CRITICAL.
- `state` (String) State derived from member entities. Values: UNKNOWN, ACTIVE.
- `uuid` (String) The UUID of the group.

## Notes

- **Exact name matching.** The `display_name` filter performs a case-sensitive exact match. Use the `turbonomic_group` resource to manage groups whose names you control.
- **Pagination is handled automatically.** The data source fetches all pages from the Turbonomic API and returns the full filtered result in a single list.
- **Reference groups in policies.** The most common use of this data source is to read a group's `uuid` and pass it to a policy or action resource.
