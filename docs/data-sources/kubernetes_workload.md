---
page_title: "turbonomic_kubernetes_workload Data Source - IBM Turbonomic"
subcategory: "Kubernetes"
description: |-
  Retrieves Turbonomic optimization recommendations for a Kubernetes WorkloadController (Deployment, StatefulSet, etc.). Returns recommended replica counts and per-container CPU/memory resource values.
---

# turbonomic_kubernetes_workload (Data Source)

Retrieves Turbonomic optimization recommendations for a Kubernetes WorkloadController (Deployment, StatefulSet, etc.). Returns recommended replica counts and per-container CPU/memory resource values.

## Example Usage

```terraform
data "turbonomic_kubernetes_workload" "example" {
  cluster   = "<cluster_name>"
  namespace = "<namespace>"
  name      = "<workload_name>"

  default_replicas = 1

  default_containers = [
    {
      name                   = "<container_name>"
      default_cpu_request    = "200m"
      default_cpu_limit      = "500m"
      default_memory_request = "256Mi"
      default_memory_limit   = "512Mi"
    }
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `cluster` (String) Name of the Kubernetes cluster target as it appears in Turbonomic (e.g. `"Kubernetes-mycluster"`). Used to disambiguate workloads with the same name in the same namespace across multiple clusters.
- `name` (String) Name of the WorkloadController resource (e.g. Deployment name).
- `namespace` (String) Kubernetes namespace the workload belongs to.

### Optional

- `default_containers` (Attributes List) Fallback resource values for containers when Turbonomic has no RESIZE recommendation. (see [below for nested schema](#nestedatt--default_containers))
- `default_replicas` (Number) Fallback replica count to use when Turbonomic has no replica recommendation.
- `kind` (String) Workload kind hint (e.g. `"DEPLOYMENT_INFO"`, `"STATEFUL_SET_INFO"`). Accepted but not used for filtering: the Turbonomic API does not return `controllerType` in WorkloadController search results, so kind-based disambiguation is not possible. Provided for documentation purposes and future API compatibility.

### Read-Only

- `action_mode` (String) Mode of the pending action (e.g. `MANUAL`, `RECOMMEND`).
- `action_state` (String) State of the pending action (e.g. `READY`).
- `action_type` (String) Type of the pending action (`RESIZE`, `SCALE`, or empty when no action exists).
- `containers` (Attributes List) Per-container current and recommended resource values. (see [below for nested schema](#nestedatt--containers))
- `current_replicas` (Number) Current replica count reported by Turbonomic.
- `entity_uuid` (String) Turbonomic UUID of the WorkloadController entity.
- `new_replicas` (Number) Recommended replica count. Falls back to `current_replicas`, then `default_replicas`.

<a id="nestedatt--default_containers"></a>
### Nested Schema for `default_containers`

Required:

- `name` (String) Container name.

Optional:

- `default_cpu_limit` (String) Default CPU limit.
- `default_cpu_request` (String) Default CPU request (e.g. `"200m"`).
- `default_memory_limit` (String) Default memory limit.
- `default_memory_request` (String) Default memory request (e.g. `"256Mi"`).


<a id="nestedatt--containers"></a>
### Nested Schema for `containers`

Read-Only:

- `current_cpu_limit` (String) Current CPU limit.
- `current_cpu_request` (String) Current CPU request (e.g. `"200m"`).
- `current_memory_limit` (String) Current memory limit.
- `current_memory_request` (String) Current memory request (e.g. `"256Mi"`).
- `name` (String) Container name.
- `new_cpu_limit` (String) Recommended CPU limit.
- `new_cpu_request` (String) Recommended CPU request.
- `new_memory_limit` (String) Recommended memory limit.
- `new_memory_request` (String) Recommended memory request.
