---
page_title: "turbonomic_kubernetes_pod Data Source - IBM Turbonomic"
subcategory: "Kubernetes"
description: |-
  Retrieves Turbonomic optimization recommendations for a Kubernetes ContainerPod. Returns the recommended destination node for a MOVE action. Pod MOVE recommendations indicate that a pod should be rescheduled to another node for better resource utilization.
---

# turbonomic_kubernetes_pod (Data Source)

Retrieves Turbonomic optimization recommendations for a Kubernetes ContainerPod. Returns the recommended destination node for a MOVE action. Pod MOVE recommendations indicate that a pod should be rescheduled to another node for better resource utilization.

## Example Usage

```terraform
data "turbonomic_kubernetes_pod" "example" {
  cluster   = "<cluster_name>"
  namespace = "<namespace>"
  name      = "<pod_name>"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `cluster` (String) Name of the Kubernetes cluster as it appears in Turbonomic (e.g. `"Kubernetes-mycluster"`). Used to disambiguate pods with the same name across multiple clusters.
- `name` (String) Name of the Kubernetes pod. Turbonomic stores pod display names as `"<namespace>/<pod-name>"` — provide only the pod name here.
- `namespace` (String) Kubernetes namespace the pod belongs to.

### Read-Only

- `action_mode` (String) Mode of the pending action (e.g. `MANUAL`, `RECOMMEND`).
- `action_state` (String) State of the pending action (e.g. `READY`).
- `action_type` (String) Type of the pending action (`MOVE` or empty when no action exists).
- `current_node` (String) Display name of the node currently hosting the pod.
- `current_node_uuid` (String) Turbonomic UUID of the current node.
- `entity_uuid` (String) Turbonomic UUID of the ContainerPod entity.
- `new_node` (String) Display name of the recommended destination node.
- `new_node_uuid` (String) Turbonomic UUID of the recommended destination node.
